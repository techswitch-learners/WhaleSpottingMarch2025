import CustomButton from "../../formComponents/customButton/CustomButton";

export const Admin = () => {
  return (
    <div>
      <div>Admin page</div>
      <CustomButton>
        Approve
      </CustomButton>
    </div>
  );
};